<div class="navbar-fixed">
  <nav>
    <!-- Using a different navbar color for the home page than other pages -->
    <% if request.env['PATH_INFO'] == "/" %>
      <div class="nav-wrapper-home">
    <% else %>
      <div class="nav-wrapper">
    <% end %>
      <a href="<%= root_path %>" class="root-nav-button">Churner.io</a>
      <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
      	<%= render 'layouts/navigation_links' %>
      </ul>
      <% if request.env['PATH_INFO'] == "/" %>
        <ul class="side-nav" id="mobile-demo">
      <% else %>
        <ul class="side-nav side-nav-all" id="mobile-demo">
      <% end %>
      	<li><%= link_to 'Churner.io', root_path %></li>
      	<%= render 'layouts/navigation_links' %>
      </ul>
    </div>
  </nav>
</div>